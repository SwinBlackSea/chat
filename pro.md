# ChatHub 产品文档（pro.md）

> 微信式交互的 AI 模型聚合聊天**安卓客户端**：每个 AI 模型是一个"联系人"，
> 豆包、DeepSeek、GPT 各自独立聊天，就像和好友发消息一样。
> 产品名暂定 "ChatHub"，可随时改。

## 1. 产品定位

- 一句话：模型即联系人的安卓 AI 聊天客户端（微信式交互）。
- 交互范式：聊天页看最近对话 → 通讯录里找模型 → 点进联系人的独立聊天窗口。
- 对标：Chatbox、LobeChat（功能同类，交互改为微信式联系人）。
- 核心价值：
  - 零学习成本：微信心智，上手即用
  - 边界清晰：每个模型一个聊天窗口，上下文互不串扰
  - Key 自主：API Key 只存自己的服务器，不经第三方
  - 易扩展：任何 OpenAI 兼容服务几分钟接入，加模型 = 加联系人

## 2. 目标用户

- 同时使用多家模型 API 的个人开发者 / AI 重度用户
- 习惯微信式交互、想要"像聊天一样用 AI"的用户

## 3. 核心概念

| 概念 | 说明 |
| --- | --- |
| Chatbot 联系人 | 一个启用的模型即一个联系人，如 "DeepSeek 对话"（deepseek-chat）；有头像、显示名 |
| 聊天页 | 首页。最近对话列表：头像、联系人名、最后一条消息预览、时间；生成中显示动态状态 |
| 聊天窗口 | 与单个联系人的独立对话；上下文在该联系人内延续 |
| 通讯录 | 全部模型联系人，按服务商分组；点击进入聊天窗口 |
| Provider（服务商） | 模型服务提供方（DeepSeek、豆包火山方舟等），持有 baseUrl 与 API Key；添加服务商批量产生联系人 |
| 人设（system prompt） | 每个联系人可单独设置，类似好友备注/设定 |

## 4. 功能范围

### P0（MVP，第一期交付）

1. 连接服务器（安卓端）
   - 首次启动填写后端地址，连通性校验通过后进入
   - 地址可修改、可重新校验
2. Provider 管理（设置页）
   - 内置模板一键添加：DeepSeek、豆包（火山方舟）、OpenAI、Moonshot、智谱 GLM、通义千问、Ollama（本地）
   - 支持自定义 OpenAI 兼容端点（任意 baseUrl + Key）
   - 连接测试：最小请求验证 baseUrl 与 Key，返回明确结果
   - 编辑 / 删除
3. 联系人（模型）管理
   - 添加服务商时按模板默认模型自动生成联系人（可勾选）
   - 手动添加/删除联系人（如豆包接入点 `ep-xxx`）；可设置显示名与头像色
   - 启用/停用：停用的联系人从通讯录与聊天入口隐藏（历史保留）
4. 聊天页（首页）
   - 最近对话列表，按最后消息时间倒序
   - 每行：头像、联系人名、最后消息预览、时间；该联系人生成中时显示"正在回答…"
   - 点击进入聊天窗口
5. 聊天窗口
   - 流式输出（打字机效果），可一键停止
   - Markdown 渲染、代码高亮、代码块一键复制
   - 重新生成最后一条回答；复制任意消息
   - 上游报错给出明确提示（Key 无效、余额不足、限流等），不卡死
6. 联系人资料页
   - 模型信息：服务商、模型 ID、上下文长度
   - 设置人设（system prompt）
   - 清空聊天记录（重置该联系人上下文）

### P1（第二期）

- 群聊：拉多个联系人进群，同一问题多模型并排回答（微信式模型对比）
- 提示词模板库（快捷填入）
- 用量统计：按服务商/模型统计请求数与 token 消耗
- 对话导出（Markdown / JSON）
- 上下文超限自动截断策略（滑动窗口）
- 暗色主题

### P2（远期）

- 多设备同步与访问控制
- 语音输入 / TTS 朗读
- 插件与工具调用（function calling）

## 5. 关键用户流程

1. 首次使用：打开 App → 填后端地址 → 校验通过 → 添加服务商（选模板填 Key）→
   连接测试 → 自动生成联系人 → 通讯录点联系人 → 开始聊天
2. 日常使用：打开 App → 聊天页看到最近对话 → 点进去继续聊；
   想换模型 → 通讯录里点另一个联系人（各自上下文独立）
3. 加新模型：设置 → 服务商 → 添加/编辑 → 把模型添加为联系人

## 6. 设计语言（微信式极简）

- 基调：克制、功能优先、零装饰——无阴影、无渐变、无毛玻璃，只用留白与发丝分割线组织层级
- 色彩：
  - 页面底灰 `#F7F7F7`、聊天底灰 `#EDEDED`、卡片纯白 `#FFFFFF`
  - 文字：主 `#111111` / 次 `#888888` / 弱化 `#B2B2B2`；分割线 `#E5E5E5`
  - 主色微信绿 `#07C160`（按钮、链接、选中态）；自己气泡 `#95EC69` 黑字；警示红 `#FA5151`
- 形状：气泡/按钮/输入框圆角 6px，头像方角 6px；不做 16px+ 大圆角卡片
- 图标：线性 SVG（1.6px 描边），未选中灰、选中绿；禁用 emoji
- 字号：标题 17px / 名称 16px / 正文 15px / 辅助 12–13px
- 布局：全宽白色分组，组间以底色间隙分隔；组内分割线左缩进对齐文本起点
- 状态：文字 + 红点角标（未读），不用彩色徽章
- 动效：仅流式光标、"对方正在输入"、开关过渡；时长 ≤200ms
- 参考原型：prototype/index.html（实现时以其为准）

## 7. 非功能需求

- 服务端本地/自有服务器部署：docker-compose 一键启动；数据与 Key 仅存服务端 SQLite
- 安卓端支持 Android 8.0+（minSdk 26）
- 安全：Key 不写日志、不进错误信息；接口返回一律脱敏；客户端不存 Key 明文缓存
- 性能：流式首字延迟 ≈ 上游 API 延迟（后端纯转发，不做额外加工）

## 8. MVP 验收标准

- [ ] 首次启动填后端地址，校验通过后进入
- [ ] 成功添加 DeepSeek 与豆包（火山方舟）并通过连接测试，联系人生成到通讯录
- [ ] 聊天页正确显示预览与时间；点联系人进入独立聊天窗口
- [ ] 流式输出正常，中途可停止；Markdown / 代码块渲染正确，代码可复制
- [ ] 重新生成、复制消息可用
- [ ] 各联系人上下文互不影响；清空聊天记录后上下文重置
- [ ] 重启服务端后服务商、联系人、消息不丢失
- [ ] Key 错误 / 上游报错时，界面有明确错误提示
